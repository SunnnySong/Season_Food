<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ksf.seasonfood.dao.MemberDao">
	<insert id="register">
	    INSERT INTO MP_MEMBER(   ID
	   					       , PASSWORD 
                               , NAME
                               , BIRTH
                               , EMAIL 	)
	                  VALUES(    #{id} 
	                 		   , #{password}
                               , #{name}
                               , #{birth}
                               , #{email}
                               )
    </insert>
   
</mapper>